export enum PageEnum {
  // basic login path
  BASE_LOGIN = "/login",
  // basic home path
  BASE_HOME = "/center",
  DASHBOARD_PAGE = "/dashboard",
  // error page path
  ERROR_PAGE = "/exception"
}
